<div class="card-trip topic-selection">
  <%= cl_image_tag topic.photo.key, crop: :fill %>
  <div class="card-trip-infos">
    <h2><%= topic.name %></h2>
    <p><%= topic.category %></p>
  </div>
</div>
